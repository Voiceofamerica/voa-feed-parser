export const ZhUrl: string = process.env.ZH_URL || 'zh feed url not configured'
export const FaUrl: string = process.env.FA_URL || 'fa feed url not configured'
export const EnUrl: string = process.env.EN_URL || 'en feed url not configured'
